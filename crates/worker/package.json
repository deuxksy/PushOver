{
  "name": "pushover-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wasm-pack build --dev --target bundler"
  },
  "devDependencies": {
    "wrangler": "^4.77.0"
  }
}
