{
  "name": "pushover-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "wasm-pack build --dev --target web"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
