# v0.2.0 Queue + KV + R2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PushOver API를 Queue-First 아키텍처로 전환하고 KV 캐시, R2 스토리지를 통합하여 비동기 메시지 처리 파이프라인을 구축한다.

**Architecture:** Client → POST /messages → Queue 투입 → 202 Accepted 즉시 응답. Consumer Worker가 Queue에서 메시지를 소비하여 PushOver API 호출. KV는 토큰 캐시(TTL 1h)/웹훅 캐시(TTL 5m)/실패 메시지 백업(TTL 7d) 용도. R2는 이미지 첨부 및 D1 백업 스냅샷 저장.

**Tech Stack:** Rust (worker 0.7 crate), Cloudflare Workers/WASM, D1, KV, Queue, R2, OpenTofu

---

## File Structure

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `migrations/0004_v020_queue_kv.sql` | D1 스키마 변경 (image_url) |
| Modify | `crates/worker/wrangler.toml` | KV, Queue, R2 바인딩 추가 |
| Modify | `crates/worker/Cargo.toml` | `queue` feature, `base64` crate 추가 |
| Modify | `infrastructure/main.tf` | R2 bucket(이미지), Queue 리소스 |
| Create | `crates/worker/src/kv.rs` | KV 읽기/쓰기 (토큰 캐시, 웹훅 캐시, 실패 백업) |
| Create | `crates/worker/src/queue.rs` | Queue 메시지 타입, Consumer 로직 |
| Create | `crates/worker/src/r2.rs` | R2 이미지 업로드 |
| Modify | `crates/worker/src/lib.rs` | `#[event(queue)]` 핸들러, 모듈 등록 |
| Modify | `crates/worker/src/routes.rs` | send_message → Queue Producer 전환 |
| Modify | `crates/worker/src/recovery.rs` | KV 기반 메시지 본문 복원 로직 |
| Modify | `crates/worker/src/types.rs` | QueueMessage 타입, DbMessage image_url |
| Modify | `crates/worker/src/crypto.rs` | hash_token 함수 추가 |
| Modify | `crates/worker/src/db.rs` | update_message_image_url, Consumer 편의 메서드 |

---

### Task 1: D1 Migration - 스키마 변경

**Files:**
- Create: `migrations/0004_v020_queue_kv.sql`

- [ ] **Step 1: 마이그레이션 SQL 작성**

> 기존 `messages.status`는 CHECK 제약이 없으므로 'queued' 값 바로 사용 가능.
> `failed_deliveries`는 스키마 유지 (KV로 이관하는 것은 백업 저장소 변경이고 컬럼 구조는 동일).

```sql
-- 0004: v0.2.0 Queue + KV + R2 스키마 변경

-- messages: image_url 컬럼 추가 (R2 이미지 참조)
ALTER TABLE messages ADD COLUMN image_url TEXT;
```

- [ ] **Step 2: 로컬 D1에 마이그레이션 적용**

Run: `cd crates/worker && wrangler d1 migrations apply pushover-db --local`
Expected: 마이그레이션 성공

- [ ] **Step 3: Commit**

```bash
git add migrations/0004_v020_queue_kv.sql
git commit -m "feat: v0.2.0 D1 스키마 마이그레이션 (image_url 컬럼 추가)"
```

---

### Task 2: Cargo.toml 의존성 추가

**Files:**
- Modify: `crates/worker/Cargo.toml`

- [ ] **Step 1: queue feature 활성화 + base64 crate 추가**

`crates/worker/Cargo.toml` 수정:

```toml
# 기존: worker = { version = "0.7", features = ["d1"] }
worker = { version = "0.7", features = ["d1", "queue"] }

# 새 의존성 추가:
base64 = "0.22"
```

> 참고: `worker` 0.7 crate에서 KV는 별도 feature 없이 `Env::kv()`로 접근. Queue는 `queue` feature 필요.

- [ ] **Step 2: cargo check**

Run: `cd crates/worker && cargo check`
Expected: 의존성 다운로드 후 컴파일 성공

- [ ] **Step 3: Commit**

```bash
git add crates/worker/Cargo.toml
git commit -m "feat: worker queue feature 활성화, base64 crate 추가"
```

---

### Task 3: wrangler.toml 바인딩 + OpenTofu 인프라

**Files:**
- Modify: `crates/worker/wrangler.toml`
- Modify: `infrastructure/main.tf`

- [ ] **Step 1: wrangler.toml에 KV, Queue, R2 바인딩 추가**

`crates/worker/wrangler.toml`의 기존 `[[d1_databases]]` 아래에 추가:

```toml
[[kv_namespaces]]
binding = "KV"
id = "<KV_NAMESPACE_ID>"  # tofu output으로 획득 후 입력

[[queues.producers]]
queue = "pushover-messages"
binding = "MESSAGE_QUEUE"

[[queues.consumers]]
queue = "pushover-messages"
max_batch_size = 10
max_batch_timeout = 30
max_retries = 3

[[r2_buckets]]
binding = "R2"
bucket_name = "pushover-images"
```

- [ ] **Step 2: infrastructure/main.tf에 R2, Queue 리소스 추가**

```hcl
# Message Images R2 Bucket
resource "cloudflare_r2_bucket" "pushover_images" {
  account_id = var.cloudflare_account_id
  name       = "pushover-images"
  location   = "APAC"
}

# Message Queue (wrangler에서도 생성 가능하나 IaC로 관리)
# Queue는 wrangler.toml 설정으로 자동 생성됨
```

- [ ] **Step 3: tofu plan으로 변경 확인**

Run: `cd infrastructure && tofu plan`
Expected: R2 bucket 생성 확인

- [ ] **Step 4: KV namespace ID 획득 후 wrangler.toml 업데이트**

```bash
# 기존 KV namespace가 있으면 output 확인
cd infrastructure && tofu output
# 없으면 새로 생성 후 ID 적용
```

- [ ] **Step 5: Commit**

```bash
git add crates/worker/wrangler.toml infrastructure/main.tf
git commit -m "feat: KV, Queue, R2 바인딩 및 인프라 리소스 추가"
```

---

### Task 4: types.rs - QueueMessage 타입 + DbMessage image_url

**Files:**
- Modify: `crates/worker/src/types.rs`

- [ ] **Step 1: QueueMessage 타입 및 KV 상수 추가**

`crates/worker/src/types.rs` 끝에 추가:

```rust
// ---- v0.2.0: Queue + KV + R2 ----

/// Queue에 투입되는 메시지 페이로드
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct QueueMessage {
    pub id: String,
    pub user_key: String,
    pub api_token: String,
    pub pushover_token: String,
    pub user: String,
    pub message: String,
    pub title: Option<String>,
    pub priority: Option<i32>,
    pub sound: Option<String>,
    pub device: Option<String>,
    pub url: Option<String>,
    pub url_title: Option<String>,
    pub html: Option<bool>,
    pub retry: Option<u32>,
    pub expire: Option<u32>,
    pub image_url: Option<String>,
}

/// KV 키 프리픽스
pub const KV_PREFIX_TOKEN: &str = "pushover-tokens:";
pub const KV_PREFIX_WEBHOOK: &str = "pushover-webhooks:";
pub const KV_PREFIX_FAILED: &str = "pushover-failed:";

/// KV 캐시 TTL (초)
pub const TTL_TOKEN: u64 = 3600;       // 1시간
pub const TTL_WEBHOOK: u64 = 300;      // 5분
pub const TTL_FAILED: u64 = 604800;    // 7일
```

- [ ] **Step 2: DbMessage에 image_url 필드 추가**

`DbMessage` struct에 필드 추가:

```rust
pub image_url: Option<String>,
```

- [ ] **Step 3: cargo check**

Run: `cd crates/worker && cargo check`
Expected: 컴파일 성공

- [ ] **Step 4: Commit**

```bash
git add crates/worker/src/types.rs
git commit -m "feat: QueueMessage 타입, DbMessage image_url, KV 상수 정의"
```

---

### Task 5: crypto.rs - hash_token 함수 추가

**Files:**
- Modify: `crates/worker/src/crypto.rs`

- [ ] **Step 1: hash_token 함수 추가**

`crates/worker/src/crypto.rs` 끝에 추가:

```rust
/// 토큰 해시 (KV 캐시 키로 사용, 기존 sha2 crate 재사용)
pub fn hash_token(token: &str) -> String {
    use sha2::{Sha256, Digest};
    let mut hasher = Sha256::new();
    hasher.update(token.as_bytes());
    format!("{:x}", hasher.finalize())
}
```

> 기존 `sha2` crate가 이미 `Cargo.toml`에 있으므로 추가 의존성 불필요.

- [ ] **Step 2: cargo check**

Run: `cd crates/worker && cargo check`
Expected: 컴파일 성공

- [ ] **Step 3: Commit**

```bash
git add crates/worker/src/crypto.rs
git commit -m "feat: hash_token 함수 추가 (KV 캐시 키 생성용)"
```

---

### Task 6: kv.rs - KV 캐시 모듈

**Files:**
- Create: `crates/worker/src/kv.rs`

- [ ] **Step 1: kv.rs 모듈 작성**

```rust
use worker::*;
use crate::types::{KV_PREFIX_TOKEN, KV_PREFIX_WEBHOOK, KV_PREFIX_FAILED, TTL_TOKEN, TTL_WEBHOOK, TTL_FAILED};
use crate::db::Db;
use crate::crypto::hash_token;

pub struct Kv {
    namespace: KvStore,
}

impl Kv {
    pub fn new(env: &Env) -> Result<Self> {
        let namespace = env.kv("KV")?;
        Ok(Self { namespace })
    }

    // --- Token 캐시 (D1이 원본, KV는 읽기 캐시) ---

    fn token_key(token_hash: &str) -> String {
        format!("{}{}", KV_PREFIX_TOKEN, token_hash)
    }

    /// KV에서 토큰으로 user_key 조회
    pub async fn get_cached_user_key(&self, token_hash: &str) -> Result<Option<String>> {
        let key = Self::token_key(token_hash);
        self.namespace.get(&key).text().await
    }

    /// KV에 토큰 → user_key 캐시 저장
    pub async fn cache_user_key(&self, token_hash: &str, user_key: &str) -> Result<()> {
        let key = Self::token_key(token_hash);
        self.namespace.put(&key, user_key)?
            .expiration_ttl(TTL_TOKEN)
            .execute()
            .await
    }

    /// D1에서 토큰 검증 후 KV에 캐시 (Cache-Aside)
    pub async fn validate_token(&self, db: &Db, token: &str) -> Result<Option<String>> {
        let hash = hash_token(token);

        if let Some(user_key) = self.get_cached_user_key(&hash).await? {
            return Ok(Some(user_key));
        }

        // KV miss → D1 조회
        if let Some(user_key) = db.validate_token(token).await? {
            let _ = self.cache_user_key(&hash, &user_key).await;
            Ok(Some(user_key))
        } else {
            Ok(None)
        }
    }

    // --- Webhook 캐시 ---

    fn webhook_key(user_key: &str) -> String {
        format!("{}{}", KV_PREFIX_WEBHOOK, user_key)
    }

    pub async fn get_cached_webhooks(&self, user_key: &str) -> Result<Option<String>> {
        let key = Self::webhook_key(user_key);
        self.namespace.get(&key).text().await
    }

    pub async fn cache_webhooks(&self, user_key: &str, webhooks_json: &str) -> Result<()> {
        let key = Self::webhook_key(user_key);
        self.namespace.put(&key, webhooks_json)?
            .expiration_ttl(TTL_WEBHOOK)
            .execute()
            .await
    }

    // --- 실패 메시지 백업 ---

    fn failed_key(message_id: &str) -> String {
        format!("{}{}", KV_PREFIX_FAILED, message_id)
    }

    /// 실패한 메시지 본문을 KV에 백업 (QueueMessage JSON)
    pub async fn backup_failed_message(&self, message_id: &str, body: &str) -> Result<()> {
        let key = Self::failed_key(message_id);
        self.namespace.put(&key, body)?
            .expiration_ttl(TTL_FAILED)
            .execute()
            .await
    }

    /// KV에서 실패 메시지 본문 복원
    pub async fn restore_failed_message(&self, message_id: &str) -> Result<Option<String>> {
        let key = Self::failed_key(message_id);
        self.namespace.get(&key).text().await
    }

    /// 재전송 성공 후 KV 백업 삭제
    pub async fn delete_failed_backup(&self, message_id: &str) -> Result<()> {
        let key = Self::failed_key(message_id);
        self.namespace.delete(&key).await
    }
}
```

- [ ] **Step 2: lib.rs에 모듈 등록**

`crates/worker/src/lib.rs` 상단 `mod` 목록에 추가:

```rust
mod kv;
mod queue;
mod r2;
```

> `queue.rs`, `r2.rs`는 임시 빈 파일로 생성 필요: `touch crates/worker/src/queue.rs crates/worker/src/r2.rs`

- [ ] **Step 3: cargo check**

Run: `cd crates/worker && cargo check`
Expected: 컴파일 성공

- [ ] **Step 4: Commit**

```bash
git add crates/worker/src/kv.rs crates/worker/src/lib.rs
git commit -m "feat: KV 캐시 모듈 (토큰/웹훅/실패메시지)"
```

---

### Task 7: queue.rs - Queue Consumer 모듈

**Files:**
- Create: `crates/worker/src/queue.rs`

- [ ] **Step 1: queue.rs 모듈 작성**

```rust
use worker::*;
use crate::types::QueueMessage;
use crate::pushover::PushOverClient;
use crate::db::Db;
use crate::kv::Kv;

/// 단일 Queue 메시지 소비
pub async fn consume_message(
    msg: &QueueMessage,
    env: &Env,
) -> Result<()> {
    let client = PushOverClient::from_env(env)?;
    let db = Db::from_env(env)?;
    let kv = Kv::new(env)?;

    let result = match client.send_message(
        &msg.pushover_token,
        &msg.user,
        &msg.message,
        msg.title.as_deref(),
        msg.priority,
        msg.sound.as_deref(),
        msg.device.as_deref(),
        msg.url.as_deref(),
        msg.url_title.as_deref(),
        msg.html,
        msg.retry,
        msg.expire,
    ).await {
        Ok(r) => r,
        Err(e) => {
            // 실패: KV에 메시지 본문 백업 + D1 failed_deliveries 기록
            let body = serde_json::to_string(&msg).unwrap_or_default();
            let _ = kv.backup_failed_message(&msg.id, &body).await;
            let _ = db.upsert_failed_delivery(&msg.id, &e.to_string()).await;

            // D1 메시지 상태를 failed로 업데이트
            let _ = db.update_message_status(&msg.id, "failed").await;

            return Err(e);
        }
    };

    // 성공: D1 메시지 상태 업데이트 (status=sent)
    db.update_message_status(&msg.id, "sent").await?;

    if let Some(ref receipt) = result.receipt {
        db.update_message_receipt(&msg.id, receipt).await?;
    }

    Ok(())
}

/// Batch 메시지 소비 (Cloudflare Queue 배치 처리)
pub async fn consume_batch(
    messages: &[QueueMessage],
    env: &Env,
) {
    for msg in messages {
        if let Err(e) = consume_message(msg, env).await {
            console_error!("Queue consumer error for msg {}: {}", msg.id, e);
        }
    }
}
```

- [ ] **Step 2: cargo check**

Run: `cd crates/worker && cargo check`
Expected: 컴파일 성공

- [ ] **Step 3: Commit**

```bash
git add crates/worker/src/queue.rs
git commit -m "feat: Queue Consumer 모듈 (메시지 소비/실패처리)"
```

---

### Task 8: r2.rs - R2 스토리지 모듈

**Files:**
- Create: `crates/worker/src/r2.rs`

- [ ] **Step 1: r2.rs 모듈 작성**

```rust
use std::collections::HashMap;
use worker::*;

pub struct R2 {
    bucket: Bucket,
}

impl R2 {
    pub fn new(env: &Env) -> Result<Self> {
        let bucket = env.bucket("R2")?;
        Ok(Self { bucket })
    }

    /// 이미지를 R2에 업로드
    pub async fn upload_image(
        &self,
        key: &str,
        data: &[u8],
        content_type: &str,
    ) -> Result<String> {
        let metadata = HashMap::from([
            ("content_type".to_string(), content_type.to_string()),
        ]);

        self.bucket.put(key, data.to_vec())
            .custom_metadata(metadata)
            .execute()
            .await?;

        Ok(format!("/images/{}", key))
    }

    /// 이미지 다운로드
    pub async fn get_image(&self, key: &str) -> Result<Option<Vec<u8>>> {
        let object = self.bucket.get(key).execute().await?;

        match object {
            Some(obj) => {
                if let Some(body) = obj.body() {
                    let bytes = body.bytes().await?;
                    Ok(Some(bytes))
                } else {
                    Ok(None)
                }
            }
            None => Ok(None),
        }
    }
}
```

> **검증 완료:** worker 0.7.5 crate에서 R2는 `env.bucket("NAME")` → `Result<Bucket>`.
> `Bucket::put()`는 builder 반환, `.custom_metadata(HashMap).execute().await?`.
> `Object::body()`는 `Option<Body>`, `Body::bytes().await?`로 `Vec<u8>` 획득.

- [ ] **Step 2: cargo check**

Run: `cd crates/worker && cargo check`
Expected: 컴파일 성공. `env.bucket("R2")` API 확인.

- [ ] **Step 3: Commit**

```bash
git add crates/worker/src/r2.rs
git commit -m "feat: R2 스토리지 모듈 (이미지 업로드/다운로드)"
```

---

### Task 9: lib.rs - Queue 이벤트 핸들러 등록

**Files:**
- Modify: `crates/worker/src/lib.rs`

- [ ] **Step 1: Queue message 이벤트 핸들러 추가**

`crates/worker/src/lib.rs`에 `use` 및 이벤트 핸들러 추가:

```rust
// 기존 use 문 아래에 추가
use crate::queue::consume_batch;
use crate::types::QueueMessage;

// 기존 #[event(scheduled)] 아래에 추가
#[event(queue)]
pub async fn queue(
    message_batch: MessageBatch<QueueMessage>,
    env: Env,
    _ctx: Context,
) -> Result<()> {
    let messages: Vec<QueueMessage> = message_batch
        .messages()
        .into_iter()
        .map(|m| m.body().clone())
        .collect();

    consume_batch(&messages, &env).await;
    Ok(())
}
```

> **검증 완료:** worker 0.7.5에서 `MessageBatch::messages()`는 메서드, `Result<Vec<Message<T>>>` 반환. `Message::body()`는 `&T` 참조 반환. `clone()` 필요.

- [ ] **Step 2: cargo check**

Run: `cd crates/worker && cargo check`
Expected: `MessageBatch` 제네릭 타입 호환성 확인

- [ ] **Step 3: Commit**

```bash
git add crates/worker/src/lib.rs
git commit -m "feat: #[event(queue)] 핸들러 등록"
```

---

### Task 10: routes.rs - send_message을 Queue Producer로 전환

**Files:**
- Modify: `crates/worker/src/routes.rs`
- Modify: `crates/worker/src/db.rs`

- [ ] **Step 1: db.rs에 update_message_image_url 메서드 추가**

```rust
pub async fn update_message_image_url(&self, id: &str, image_url: &str) -> Result<()> {
    self.d1.prepare("UPDATE messages SET image_url = ? WHERE id = ?")
        .bind(&[JsValue::from_str(image_url), JsValue::from_str(id)])?
        .run()
        .await?;
    Ok(())
}
```

- [ ] **Step 2: routes.rs의 send_message을 Queue Producer로 교체**

기존 `send_message` 함수 전체를 다음으로 교체:

```rust
/// POST /api/v1/messages - Queue에 메시지 투입 (Producer)
pub async fn send_message(
    mut req: Request,
    ctx: RouteContext<()>,
) -> worker::Result<Response> {
    let token = match extract_token(&req) {
        Ok(t) => t,
        Err(_) => return unauthorized_response("Missing or invalid Authorization header"),
    };

    // KV 캐시 우선 토큰 검증 (Cache-Aside)
    let db = Db::new(&ctx)?;
    let kv = crate::kv::Kv::new(&ctx.env)?;
    let user_key = match kv.validate_token(&db, &token).await? {
        Some(key) => key,
        None => return unauthorized_response("Invalid or inactive token"),
    };

    let body: serde_json::Value = req.json().await?;

    let user = body.get("user")
        .and_then(|v| v.as_str())
        .ok_or_else(|| Error::from("Missing 'user' field"))?;
    let message = body.get("message")
        .and_then(|v| v.as_str())
        .ok_or_else(|| Error::from("Missing 'message' field"))?;
    let pushover_token = body.get("token")
        .and_then(|v| v.as_str())
        .ok_or_else(|| Error::from("Missing 'token' field (PushOver app token)"))?;

    let msg_id = uuid::Uuid::new_v4().to_string();

    // 이미지 처리 (base64 → R2 업로드)
    let image_url = if let Some(image_b64) = body.get("image").and_then(|v| v.as_str()) {
        let r2 = crate::r2::R2::new(&ctx.env)?;
        let image_key = format!("msg/{}/image.png", &msg_id);
        match base64::engine::general_purpose::STANDARD.decode(image_b64) {
            Ok(data) => {
                match r2.upload_image(&image_key, &data, "image/png").await {
                    Ok(url) => Some(url),
                    Err(_) => None,
                }
            }
            Err(_) => None,
        }
    } else {
        None
    };

    // 메시지 필드 추출
    let title = body.get("title").and_then(|v| v.as_str());
    let priority = body.get("priority").and_then(|v| v.as_i64()).map(|p| p as i32);
    let sound = body.get("sound").and_then(|v| v.as_str());
    let device = body.get("device").and_then(|v| v.as_str());
    let url_field = body.get("url").and_then(|v| v.as_str());
    let url_title = body.get("url_title").and_then(|v| v.as_str());
    let html = body.get("html").and_then(|v| v.as_bool());
    let retry = body.get("retry").and_then(|v| v.as_u64()).map(|r| r as u32);
    let expire = body.get("expire").and_then(|v| v.as_u64()).map(|e| e as u32);

    // D1에 메시지 저장 (status=queued)
    db.insert_message(
        &msg_id, &user_key, message, title,
        priority.unwrap_or(0), sound, device,
        url_field, url_title, html.unwrap_or(false),
        "queued", None, Some(&token),
    ).await?;

    if let Some(ref img_url) = image_url {
        db.update_message_image_url(&msg_id, img_url).await?;
    }

    // Queue에 메시지 투입
    let queue_msg = crate::types::QueueMessage {
        id: msg_id.clone(),
        user_key: user_key.clone(),
        api_token: token.to_string(),
        pushover_token: pushover_token.to_string(),
        user: user.to_string(),
        message: message.to_string(),
        title: title.map(String::from),
        priority,
        sound: sound.map(String::from),
        device: device.map(String::from),
        url: url_field.map(String::from),
        url_title: url_title.map(String::from),
        html,
        retry,
        expire,
        image_url: image_url.clone(),
    };

    // 단일 메시지 전송 (send는 Serialize 구현체를 소유권으로 받음)
    ctx.env.queue("MESSAGE_QUEUE")?
        .send(queue_msg)
        .await?;

    Ok(with_cors(Response::from_json(&serde_json::json!({
        "status": "queued",
        "message_id": msg_id,
        "request": uuid::Uuid::new_v4().to_string()
    }))?.with_status(202)))
}
```

> **검증 완료:** worker 0.7.5에서 `env.queue("NAME")?.send(msg)`는 `impl Serialize` 값을 소유권으로 받음. `&queue_msg` 참조가 아닌 `queue_msg` 직접 전달.

- [ ] **Step 3: cargo check**

Run: `cd crates/worker && cargo check`
Expected: 컴파일 성공. Queue/R2 API 메서드 호환성 확인.

- [ ] **Step 4: Commit**

```bash
git add crates/worker/src/routes.rs crates/worker/src/db.rs
git commit -m "feat: send_message을 Queue Producer로 전환 (202 Accepted)"
```

---

### Task 11: recovery.rs - KV 기반 복원 로직

**Files:**
- Modify: `crates/worker/src/recovery.rs`

- [ ] **Step 1: recovery.rs를 KV 기반 복원으로 리팩토링**

기존 `handle_failed_messages` 함수를 다음으로 교체. **반환 타입은 `Result<Response>` 유지** (기존 코드와 동일):

```rust
use worker::*;
use crate::db::Db;
use crate::kv::Kv;
use crate::pushover::PushOverClient;
use crate::types::QueueMessage;

const MAX_RETRY_ATTEMPTS: u32 = 3;

pub async fn handle_failed_messages(
    _event: ScheduledEvent,
    env: Env,
    _ctx: ScheduleContext,
) -> Result<Response> {
    let db = Db::from_env(&env)?;
    let kv = Kv::new(&env)?;
    let client = PushOverClient::from_env(&env)?;

    let failed = db.get_failed_deliveries(MAX_RETRY_ATTEMPTS).await?;

    for delivery in failed {
        // KV에서 메시지 본문 복원 (QueueMessage JSON)
        let body = match kv.restore_failed_message(&delivery.message_id).await? {
            Some(b) => b,
            None => {
                // KV TTL 만료 → 복원 불가, 영구 실패 처리
                db.update_message_status(&delivery.message_id, "permanently_failed").await?;
                db.delete_failed_delivery(&delivery.message_id).await?;
                continue;
            }
        };

        let msg: QueueMessage = match serde_json::from_str(&body) {
            Ok(m) => m,
            Err(_) => {
                let _ = db.delete_failed_delivery(&delivery.message_id).await;
                continue;
            }
        };

        match client.send_message(
            &msg.pushover_token, &msg.user, &msg.message,
            msg.title.as_deref(), msg.priority, msg.sound.as_deref(),
            msg.device.as_deref(), msg.url.as_deref(), msg.url_title.as_deref(),
            msg.html, msg.retry, msg.expire,
        ).await {
            Ok(result) => {
                db.update_message_status(&delivery.message_id, "sent").await?;
                if let Some(ref receipt) = result.receipt {
                    db.update_message_receipt(&delivery.message_id, receipt).await?;
                }
                db.delete_failed_delivery(&delivery.message_id).await?;
                kv.delete_failed_backup(&delivery.message_id).await?;
            }
            Err(e) => {
                db.upsert_failed_delivery(&delivery.message_id, &e.to_string()).await?;

                // 기존 attempt_count + 1이 MAX에 도달하면 영구 실패
                if delivery.attempt_count + 1 >= MAX_RETRY_ATTEMPTS as i32 {
                    db.update_message_status(&delivery.message_id, "permanently_failed").await?;
                    db.delete_failed_delivery(&delivery.message_id).await?;
                    // KV는 TTL 만료로 자동 정리
                }
            }
        }
    }

    Response::ok("Recovery completed")
}

/// 수동 재시도 (기존 유지, KV 백업에서 복원)
pub async fn retry_message(
    message_id: &str,
    env: &Env,
) -> Result<bool> {
    let db = Db::from_env(env)?;
    let kv = Kv::new(env)?;
    let client = PushOverClient::from_env(env)?;

    // KV에서 백업된 메시지 본문 복원 시도
    let body = match kv.restore_failed_message(message_id).await? {
        Some(b) => b,
        None => {
            // KV 없으면 D1에서 직접 조회 (fallback)
            let msg = match db.get_message_by_id(message_id).await? {
                Some(m) => m,
                None => return Ok(false),
            };
            let api_token = match msg.api_token.as_deref() {
                Some(t) if !t.is_empty() => t,
                _ => return Ok(false),
            };

            let retry = msg.retry.map(|r| r as u32);
            let expire = msg.expire.map(|e| e as u32);

            match client.send_message(
                api_token, &msg.user_key, &msg.message,
                msg.title.as_deref(), Some(msg.priority), msg.sound.as_deref(),
                msg.device.as_deref(), msg.url.as_deref(), msg.url_title.as_deref(),
                Some(msg.html != 0), retry, expire,
            ).await {
                Ok(result) => {
                    db.update_message_status(&msg.id, "sent").await?;
                    if let Some(ref receipt) = result.receipt {
                        db.update_message_receipt(&msg.id, receipt).await?;
                    }
                    db.delete_failed_delivery(&msg.id).await?;
                    Ok(true)
                }
                Err(e) => {
                    db.upsert_failed_delivery(&msg.id, &e.to_string()).await?;
                    Ok(false)
                }
            }
        }
    };

    let msg: QueueMessage = match serde_json::from_str(&body) {
        Ok(m) => m,
        Err(_) => return Ok(false),
    };

    match client.send_message(
        &msg.pushover_token, &msg.user, &msg.message,
        msg.title.as_deref(), msg.priority, msg.sound.as_deref(),
        msg.device.as_deref(), msg.url.as_deref(), msg.url_title.as_deref(),
        msg.html, msg.retry, msg.expire,
    ).await {
        Ok(result) => {
            db.update_message_status(&msg.id, "sent").await?;
            if let Some(ref receipt) = result.receipt {
                db.update_message_receipt(&msg.id, receipt).await?;
            }
            db.delete_failed_delivery(&msg.id).await?;
            kv.delete_failed_backup(&msg.id).await?;
            Ok(true)
        }
        Err(e) => {
            db.upsert_failed_delivery(&msg.id, &e.to_string()).await?;
            Ok(false)
        }
    }
}
```

- [ ] **Step 2: cargo check**

Run: `cd crates/worker && cargo check`
Expected: 컴파일 성공

- [ ] **Step 3: Commit**

```bash
git add crates/worker/src/recovery.rs
git commit -m "feat: Cron 복구 로직을 KV 기반 메시지 복원으로 전환"
```

---

### Task 12: Cron D1 백업 스냅샷 (R2)

**Files:**
- Modify: `crates/worker/src/recovery.rs`
- Modify: `crates/worker/wrangler.toml`

- [ ] **Step 1: wrangler.toml에 백업용 R2 bucket 추가**

`crates/worker/wrangler.toml`에 추가:

```toml
[[r2_buckets]]
binding = "BACKUP_R2"
bucket_name = "pushover-backups"
```

- [ ] **Step 2: infrastructure/main.tf에 백업 R2 bucket 리소스 추가**

```hcl
resource "cloudflare_r2_bucket" "pushover_backups" {
  account_id = var.cloudflare_account_id
  name       = "pushover-backups"
  location   = "APAC"
}
```

- [ ] **Step 3: recovery.rs에 D1 백업 로직 추가**

`recovery.rs` 상단에 `use crate::r2::R2;` 추가 후, `handle_failed_messages` 시작 부분에 백업 로직 삽입:

```rust
// D1 백업 스냅샷 (Cron 호출 시 주간 1회 또는 매 실행)
if let Ok(backup_bucket) = env.bucket("BACKUP_R2") {
    let timestamp = chrono::Utc::now().format("%Y%m%d_%H%M%S");
    let backup_key = format!("d1-backups/{}.json", timestamp);

    // D1 전체 export 후 R2 저장
    // 참고: Worker 환경에서 D1 export는 SQL 쿼리로 덤프
    let db = Db::from_env(&env)?;
    if let Ok(messages) = db.export_all_messages().await {
        let data = serde_json::to_vec(&messages).unwrap_or_default();
        let _ = backup_bucket.put(&backup_key, data)
            .custom_metadata(HashMap::from([("type".to_string(), "d1-snapshot".to_string())]))
            .execute()
            .await;
    }
}
```

> **참고:** `chrono` crate가 프로젝트에 없으면 `worker` crate의 타임스탬프 유틸리티로 대체. `export_all_messages()`는 `db.rs`에 새로 추가 필요. 빌드 시점에 확인.

- [ ] **Step 4: cargo check**

Run: `cd crates/worker && cargo check`
Expected: 컴파일 성공

- [ ] **Step 5: Commit**

```bash
git add crates/worker/src/recovery.rs crates/worker/wrangler.toml infrastructure/main.tf
git commit -m "feat: Cron D1 백업 스냅샷 (R2 저장)"
```

---

### Task 13: 빌드 및 통합 테스트

- [ ] **Step 1: 전체 빌드**

Run: `cd crates/worker && cargo build --target wasm32-unknown-unknown --release`
Expected: WASM 빌드 성공

- [ ] **Step 2: wrangler dev 로컬 테스트**

Run: `cd crates/worker && wrangler dev --local`
Expected: Worker가 로컬에서 정상 시작

- [ ] **Step 3: API 테스트 스크립트 실행**

Run: `make test-api`
Expected: 기존 테스트 통과 (202 응답 확인)

- [ ] **Step 4: wrangler deploy (dev 환경)**

Run: `cd crates/worker && wrangler deploy`
Expected: 배포 성공

- [ ] **Step 5: dev 환경 Dashboard 테스트**

Run: `make dashboard-test-dev`
Expected: E2E 테스트 통과

- [ ] **Step 6: 최종 Commit + Tag**

```bash
git add -A
git commit -m "feat: v0.2.0 Queue + KV + R2 통합 완료"
git tag v0.2.0
```

---

## 의존성 순서

```
Task 1 (D1 Migration)
  ↓
Task 2 (Cargo.toml) ← Task 3 (wrangler.toml + OpenTofu) 병렬 가능
  ↓
Task 4 (types.rs: QueueMessage, DbMessage image_url)
  ↓
Task 5 (crypto.rs: hash_token)
  ↓
Task 6 (kv.rs) ← Task 7 (queue.rs) ← Task 8 (r2.rs) 병렬 가능
  ↓
Task 9 (lib.rs: #[event(queue)])
  ↓
Task 10 (routes.rs: Producer 전환)
  ↓
Task 11 (recovery.rs: KV 복원)
  ↓
Task 12 (Cron D1 백업 스냅샷 → R2)
  ↓
Task 13 (통합 테스트)
```
